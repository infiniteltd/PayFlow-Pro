import React, { Fragment } from 'react';
import Logo from '../utilities/logo';

const footer = () => {
    return (
        <Fragment>
            <div>
                <Logo />
            </div>
        </Fragment>
    );
};

export default footer;
